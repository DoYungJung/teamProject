<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.restaurant.service.impl.RestaurantMapper">

	<resultMap id="restaurant" type="egovframework.example.sample.restaurant.service.RestaurantVO">
		<!-- 필요없는 부분은 제거(User Table) -->
		<result property="userId" column="user_id"/>
		<result property="userPw" column="user_pw"/>
		<result property="userNm" column="user_name"/>
		<result property="useYn" column="use_yn"/>
		
		<!-- 필요없는 부분은 제거(Review Table) -->
		<result property="reviewNo" column="review_no"/>
		<result property="restaurantNo" column="restaurant_no"/>
		<result property="title" column="title"/>
		<result property="content" column="content"/>
		<result property="writerNm" column="writer_name"/>
		<result property="writerId" column="writer_id"/>
		<result property="regDate" column="reg_date"/>
		<result property="modDate" column="mod_date"/>
		<result property="count" column="count"/>
		
		
		
	</resultMap>
	
	
	<!-- 각 쿼리의 내부 SQL 쿼리 수정할 필요 있음 -->
	<insert id="insertRestaurant" parameterType="restaurantVO">

			INSERT INTO SAMPLE
				( ID
				  , NAME
				  , DESCRIPTION
				  , USE_YN
				  , REG_USER )
			VALUES ( #{id}
				  , #{name}
				  , #{description}
				  , #{useYn}
				  , #{regUser} )

	</insert>

	<update id="updateRestaurant">

			UPDATE SAMPLE
			SET ID=#{id}
				, NAME=#{name}
				, DESCRIPTION=#{description}
				, USE_YN=#{useYn}
				  WHERE ID=#{id}

	</update>

	<delete id="deleteRestaurant">

			DELETE FROM SAMPLE
			WHERE ID=#{id}

	</delete>

	<select id="selectRestaurant" resultMap="restaurant">

			SELECT
				ID, NAME, DESCRIPTION, USE_YN, REG_USER
			FROM SAMPLE
			WHERE ID=#{id}

	</select>

	<select id="selectRestaurantList" parameterType="restaurantVO" resultType="egovMap">

			SELECT
				ID, NAME, DESCRIPTION, USE_YN, REG_USER
			FROM SAMPLE
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND	ID LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	NAME LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
			ORDER BY ID ASC
			LIMIT #{recordCountPerPage} OFFSET #{firstIndex}
	</select>

	<select id="selectRestaurantListTotCnt" parameterType="restaurantVO" resultType="int">

			SELECT COUNT(*) totcnt
			FROM SAMPLE
			WHERE 1=1
			<if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND ID LIKE '%' || #{searchKeyword} || '%'
					</when>
		            <when test="searchCondition == 1">
						AND	NAME LIKE '%' || #{searchKeyword} || '%'
					</when>
				</choose>
			</if>
	</select>

</mapper>